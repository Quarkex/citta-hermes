name: citta
version: 1.0.0
description: >
  Attention bridge — injects the Brain's current attention before each LLM call
  and posts the context back for the Brain to attend to, via the native
  transform_context hook. Asynchronous, never blocks. Fail-open.
author: Quarkex
kind: standalone
