name: citta
version: 1.0.0
description: >
  Binds a Hermes agent to a Brain via the native transform_context hook: injects
  the Brain's current attention each turn and, if the Brain is unavailable,
  cancels the turn. No action without a Brain (fail-closed).
author: Quarkex
kind: standalone
